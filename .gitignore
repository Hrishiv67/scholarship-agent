# Secrets — never commit these
.env
*.env
secrets.json

# Python
.venv/
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.pytest_cache/

# Regenerated files and run logs (covered by email digest)
# NOTE: outputs/screenshots/ is committed on purpose - it is your proof of what
# was actually submitted (each submitted app has a *_post.png confirmation shot).
outputs/opportunities_raw.json
outputs/run_logs/

# Portal session cookies — contain auth tokens, never commit
profile/sessions/

# OS
.DS_Store
Thumbs.db
desktop.ini

# VS Code
.vscode/
